export const selectIsLoading = ({ options }) => options.isLoading;
export const selectSearch = ({ options }) => options.search;
export const selectOrderBy = ({ options }) => options.orderBy;
export const selectTodos = ({ todos }) => todos;
export const selectEditId = ({ options }) => options.editId;
